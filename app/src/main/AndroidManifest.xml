<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.anddev.paymentsprototype">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LiveOrders"></activity>
        <activity android:name=".SelectFiles" />
        <activity android:name=".feedback" />
        <activity android:name=".Setting" />
        <activity android:name=".PaymentsActivity" />
        <activity android:name=".PageSetting"
            android:windowSoftInputMode="adjustResize"/>
        <activity android:name=".PreferredShop" />
        <activity android:name=".PaymentSelected" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.VIBRATE" />

</manifest>